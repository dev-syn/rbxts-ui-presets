import { OnStart, Service } from '@flamework/core';

/**
 * A Handler that handles {'UIPresets' Component} type.
 */
@Service()
export class ComponentHandler implements OnStart {

	onStart(): void {
		throw new Error('Method not implemented.');
	}

	createComponent() {

	}

}